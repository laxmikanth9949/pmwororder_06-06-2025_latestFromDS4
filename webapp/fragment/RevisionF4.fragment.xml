<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<SelectDialog title="{i18n>Revision}" class="sapUiPopupWithPadding" items="{oRevisionModel>/}" search="onRevSearch"
		confirm="onRevConfirm" liveChange="onRevSearch" cancel="onRevCancel">
		<StandardListItem title="{oRevisionModel>Revnr}: {oRevisionModel>Revtx}"  description="{oRevisionModel>Revbd} - {oRevisionModel>Reved}"/>
	</SelectDialog>	
<!--	<TableSelectDialog title="{i18n>Revision}" liveChange="onRevSearch" search="onRevSearch" confirm="onRevConfirm" cancel="onRevCancel" items="{oRevisionModel>/}">
		<ColumnListItem>
			<cells>
				<Text text="{oRevisionModel>Revnr} : {oRevisionModel>Revtx}" />
				<Text text="{oRevisionModel>Revbd}" />
				<Text text="{oRevisionModel>Reved}" />
				
			</cells>
		</ColumnListItem>
		<columns>
			<Column minScreenWidth="Phone">
				<header>
					<Text text="{i18n>Revision}" />
				</header>
			</Column>
			<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Start">
				<header>
					<Text text="{i18n>StartDate}" />
				</header>
			</Column>
			<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Start">
				<header>
					<Text text="{i18n>EndDate}" />
				</header>
			</Column>
		</columns>
	</TableSelectDialog>	-->
</core:FragmentDefinition>

